name: Jekyll Deployment

on:
  push

jobs:
  jekyll:
    runs-on: ubuntu-16.04
    steps:
    - uses: actions/checkout@v2
    - name: Checkout submodules
      uses: textbook/git-checkout-submodule-action@2.1.1
    - name: debug output
      run: ls -l
    - name: remove what we don't need
      run: rm -rfv ansible-labs-summit2020/ansible-tower-advanced ansible-labs-summit2020/ansible-tower-getting-started

    # Standard usage
    - uses:  helaili/jekyll-action@2.0.0
      env:
        JEKYLL_PAT: ${{ secrets.JEKYLL_PAT }}
